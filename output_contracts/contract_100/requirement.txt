This smart contract is a [withdrawal contract] designed for managing token withdrawals for users. The main functions include [whitelisting addresses, setting withdrawal values, checking token balances, and enabling withdrawals]. The main states of the contract include [Whitelisting Phase, Withdrawal Phase, and Cap Reached]. The process logic for the transition of states is as follows: [First, the admin whitelists addresses for larger withdrawals. Users can then withdraw tokens, with the amount varying based on their whitelist status, until the cap is reached or the balance is insufficient]. The main variables include [token_contract, admin, withdraw_value, capReached, and withdrawed_addresses]. The main functions include [whitelist management, withdraw, and balance checks]. The main events include [address whitelisted, withdrawal successful, and cap status updated].