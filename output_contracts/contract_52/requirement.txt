This smart contract is a [token contract] for Chamacoin (symbol: CHMA). The main functions include [transferring tokens, approving token allowances, and querying balances]. The primary states of the contract encompass [Token Creation, Normal Operations, and Token Transfer]. The logic for transitioning states is as follows: [Upon contract deployment, the total supply of tokens is created and assigned to the owner's address. Users can then transfer tokens among each other, approve other users to spend tokens on their behalf, and check their balances and allowances]. The main variables include [symbol, name, decimals, totalSupply, balances, and allowed]. The main functions consist of [transfer, approve, transferFrom, and allowance]. The main events are [Transfer and Approval].